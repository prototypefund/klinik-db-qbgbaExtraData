image: rocker/r-tidyverse:4.1.1

before_script:
  - R -e 'devtools::install_github("r-lib/pkgdown")'
  - R -e 'devtools::install()'

pages:
  stage: deploy
  script:
    - R -e "dir(); getwd(); sessionInfo(); pkgdown::build_site()"
  artifacts:
    paths:
      - public
  only:
    - master

